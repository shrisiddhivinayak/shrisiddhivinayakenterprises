let nav_toggle = document.querySelector(".navContainer");

function toggleOn() {
  nav_toggle.setAttribute("data-toggle", "true");
}

function toggleOff() {
  nav_toggle.setAttribute("data-toggle", "false");
}
